
    <% content_for :utility_bar do %>
        <div class="util-head">
            <div class="container">
                <%= link_to new_event_path, :class => "btn" do %>
                    <span class="glyphicon glyphicon-plus"></span> Add Event
                <% end %>
                <ul class="list-unstyled" role="tablist">
                    <li role="presentation" class="active"><a class="btn" href="#upcoming" aria-controls="upcoming" role="tab" data-toggle="tab"><span class="glyphicon glyphicon-exclamation-sign"></span> Upcoming</a></li>
                    <li role="presentation"><a class="btn" href="#recent" aria-controls="recent" role="tab" data-toggle="tab"><span class="glyphicon glyphicon-ok-sign"></span> Recent</a></li>
                </ul>
            </div>
        </div>
    <% end %>

<section class="section">
    <div class="container">
        <div class="tab-content">
            <div role="tabpanel" class="tab-pane active" id="upcoming">
                <%= render partial: 'list', locals: { events: @upcoming } %>
            </div>
            <div role="tabpanel" class="tab-pane" id="recent">
                <%= render partial: 'list', locals: { events: @recent } %>
            </div>
        </div>
    </div>
</section>
