<section class="section">
    <div class="container">
        <h1 class="section-title">Add Resource</h1>
        <%= form_for(@resource, :html => {:class => "form-horizontal col-md-offset-3 col-md-6" }) do |f| %>
            <% if @resource.errors.any? %>
                <div id="error_explanation">
                    <h2><%= pluralize(@resource.errors.count, "error") %> prohibited this resource from being saved:</h2>

                    <ul>
                        <% @resource.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <%= render partial: 'topics/form', locals: { f: f } %>

            <div class="form-group">
                <%= f.submit :class => "btn btn-block btn-lg white bg-charcoal" %>
            </div>
        <% end %>
    </div>
</section>
